"use client"

import { FilledButton, FilledButtonProps } from "dodam-design-system/components"

const TestFilledButton = (props: FilledButtonProps) => {
  return (
    <FilledButton {...props}/>
  )
}

export default TestFilledButton