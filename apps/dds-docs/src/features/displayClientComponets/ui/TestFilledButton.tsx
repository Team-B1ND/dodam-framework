"use client"

import { FilledButton, FilledButtonProps } from "@b1nd/dodam-design-system";

const TestFilledButton = (props: FilledButtonProps) => {
  return (
    <FilledButton {...props}/>
  )
}

export default TestFilledButton