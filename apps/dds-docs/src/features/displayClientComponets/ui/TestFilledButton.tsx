"use client"

import { FilledButton, FilledButtonProps } from "@dds-web/components"

const TestFilledButton = (props: FilledButtonProps) => {
  return (
    <FilledButton {...props}/>
  )
}

export default TestFilledButton